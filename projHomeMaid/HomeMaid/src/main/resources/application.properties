# ====================================================================
# Configura��es do MongoDB
# ====================================================================
spring.data.mongodb.uri=mongodb://localhost:27017/smart_home_controller_mongodb
spring.data.mongodb.database=smart_home_controller_mongodb

# ====================================================================
# Configura��es do Cassandra
# ====================================================================
spring.cassandra.keyspace-name=smart_home_controller_cassandra
spring.cassandra.contact-points=localhost
spring.cassandra.port=9042
spring.cassandra.local-datacenter=datacenter1

# ====================================================================
# Configura��es do Servidor
# ====================================================================
# Porta onde a aplica��o ser� executada (opcional)
server.port=8080

# ====================================================================
# Configura��es de Logs
# ====================================================================
# N�vel de log (opcional, �til para reduzir verbosidade)
logging.level.root=WARN
logging.level.org.springframework=INFO

# ====================================================================
# Configura��es de Teste (caso deseje desabilitar o servidor web para testes)
# ====================================================================
# Desabilita o servidor web durante os testes (apenas se necess�rio)
spring.main.web-application-type=servlet
